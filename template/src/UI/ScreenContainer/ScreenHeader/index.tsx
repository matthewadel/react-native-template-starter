import React from 'react';
import {View} from 'UI';

export interface IScreenHeader {
  title?: string;
  showLogo?: string;
}

export const ScreenHeader = (props: IScreenHeader) => {
  return <View></View>;
};
